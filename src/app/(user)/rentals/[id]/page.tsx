export default function RentalDetailPage({ params }: { params: { id: string } }) {
        return (
                <div>
                        <h1>Chi tiết địa điểm cắm trại: {params.id}</h1>
                </div>
        );
} 